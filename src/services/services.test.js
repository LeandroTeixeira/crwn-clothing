import { render } from '@testing-library/react';
import * as React from 'react';
// import { setGlobalContextData, Provider } from './provider';
import shopData from './shopData.json';

describe('Services - Provider', () => {
  it('1 - Global Context', () => {
    // const globalContextData = setGlobalContextData();
    // const {
    //   dropdownIsOpen,
    //   setCartItems,
    //   cartItems,
    //   addCartItem,
    //   removeCartItem,
    //   decreaseCartItem,
    //   setDropdownIsOpen,
    //   toggleDropdown,
    // } = globalContextData;
    // expect(globalContextData.cartItems).toHaveLength(0);
    // globalContextData.setCartItems([]);
    // globalContextData.setCartItems([1, 2, 3]);
    // globalContextData.setCartItems([1, 2, 3]);
    // console.log(globalContextData.cartItems);
  });
});
